import Slide from '../Slide'
import UnknownPage from '../UnknownPage'

export default function UnknownRoutePage() {
  return (
    <Slide id="unknown-route">
      <UnknownPage type="route" />
    </Slide>
  )
}